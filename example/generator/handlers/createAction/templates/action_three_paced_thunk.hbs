

//region {{titleCase actionName}}

export const {{constantCase actionName}} = `${ACTION_PREFIX}{{constantCase actionName}}`;
function {{camelCase actionName}}Start() {
    return { type: {{constantCase actionName}} };
}

export const {{constantCase actionName}}_SUCCESS = `${ACTION_PREFIX}{{constantCase actionName}}_SUCCESS`;
function {{camelCase actionName}}Success(data) {
    return { type: {{constantCase actionName}}_SUCCESS, data };
}

export const {{constantCase actionName}}_FAILURE = `${ACTION_PREFIX}{{constantCase actionName}}_FAILURE`;
function {{camelCase actionName}}Failure(error) {
    return { type: {{constantCase actionName}}_FAILURE, error };
}

export function {{camelCase actionName}}({{params}}) {
    return (dispatch) => {
        dispatch({{camelCase actionName}}Start());
        //TODO: request
        const data = {};
        setTimeout(() => dispatch({{camelCase actionName}}Success(data)), 500);
    }
}

//endregion$1